{
  "name": "sequelize-example",
  "version": "1.0.0",
  "description": "1. npm install 2. node server.js 2. Hit the POST endpoint for User to create a user 3. Hit the POST endpoint for Landscape to create a Landscape (make sure to include the user_id) for an existing user 4. Hit the GET endpoints to see the magic!",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node server.js",
    "watch": "nodemon server.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "bcrypt": "^5.1.0",
    "dotenv": "^16.0.3",
    "express": "^4.18.2",
    "express-handlebars": "^7.0.7",
    "express-session": "^1.17.3",
    "faker": "^5.5.3",
    "multer": "^1.4.5-lts.1",
    "mysql2": "^3.3.5",
    "sequelize": "^6.32.0",
    "watch": "^0.13.0"
  }
}
